apiVersion: v1
kind: Service
metadata:
  name: {{ include "sake-hack.fullname" . }}
  labels:
    {{- include "sake-hack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sake-hack.selectorLabels" . | nindent 4 }}
